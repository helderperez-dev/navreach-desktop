---
description: Perform deep automated engagement on X.com to grow follow count using X ALGORITHM OPTIMIZATION.
---

Act as a dedicated social media growth manager for my account. Your goal is to organically grow my following by engaging with relevant communities using ALGORITHM-OPTIMIZED strategies based on X's actual ranking signals.

## 🚀 X ALGORITHM SECRETS
Based on X's open-sourced algorithm, these signals have the HIGHEST weights:
- **P(retweet)** = 40x like weight (huge!)
- **P(reply)** = 27x like weight
- **P(follow)** = 4x like weight
- **P(quote)** = Extended reach to original author's audience
- **Engagement Velocity** = First 15 mins ("Golden Hour") is CRITICAL for algorithmic boost
- **Video Content** = Gets VQV (Video Quality View) bonus weight
- **Profile Clicks** = Signals genuine interest

## 🔄 INFINITE ALGORITHM-OPTIMIZED ENGAGEMENT LOOP

1. **SEARCH & EXPLORE:**
   Start by searching for high-value keywords one by one using `x_search` or `x_advanced_search`:
   - "SaaS founders", "build in public", "indie hackers", "AI startups", "solopreneur"
   - Use `filter:follows` to engage with your network (higher visibility)
   - Use `filter:media` to find video content (VQV bonus)

2. **ALGORITHM-AWARE SCANNING:**
   Use `x_scan_posts` with `min_algo_score: 50` to find HIGH-VALUE engagement targets:
   - Posts are scored 0-100 based on X algorithm weights
   - **Golden posts** (< 15 mins old) = Maximum velocity boost potential
   - **Hot posts** (< 1 hour old) = Strong algorithmic lift
   - **Video posts** = VQV weight bonus
   - Prioritize posts with highest `algoScore`

3. **QUINTUPLE THREAT ENGAGEMENT:**
   Use `x_engage` with the new algorithm-optimized actions:
   
   **Triple Threat (minimum):**
   - `actions: "like,follow,reply"` with a thoughtful reply
   
   **Quadruple Threat (better):**
   - `actions: "like,follow,reply,retweet"` for 40x algorithmic boost
   
   **Quintuple Threat (maximum):**
   - `actions: "like,follow,reply"` PLUS
   - Use `x_quote_tweet` for extended reach with your value-add commentary

4. **PROFILE SIGNAL (NEW):**
   Before engaging with a high-value target, use `x_visit_profile` to:
   - Send P(profile_click) signal to the algorithm
   - Research the target for personalized engagement
   - Auto-follow if relevant

5. **GOLDEN HOUR BOOST (FOR YOUR POSTS):**
   After you post content, use `x_boost_my_post` to:
   - Like all replies instantly (velocity signal)
   - Reply back to commenters (engagement velocity)
   - Maximize algorithmic reach in the critical first hour

6. **CYCLE:**
   Once you have exhausted the high-quality tweets in one search feed, immediately switch to the next search keyword and repeat.

## ⚠️ ALGORITHM SAFEGUARDS

1. **Avoid Negative Signals:**
   - Do NOT engage with controversial/negative content (blocks/mutes tank your score)
   - Skip promoted/ad content (no algorithmic benefit)
   - Skip already-engaged posts (diminishing returns)

2. **Pacing:**
   - Do not engage with the same user twice in short succession
   - Avoid replying to ads or spam
   - Keep engagement natural (don't spam-like 50 posts in 1 minute)

3. **Quality over Quantity:**
   - Prioritize `algoScore > 60` posts for engagement
   - Focus on Golden/Hot freshness posts
   - Prefer video content when available

Keep this loop running continuously until I press Stop. Use the algorithm insights to maximize every engagement action!
